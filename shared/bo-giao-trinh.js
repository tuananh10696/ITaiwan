// =============================================================
// BỘ GIÁO TRÌNH ĐANG HIỆN
// =============================================================
// Hệ thống dựng sẵn để chứa nhiều bộ giáo trình; bản này phát hành với một bộ (Thời Đại).
// Dùng chung cho cả trình duyệt (Vite) lẫn script Node (gen-sitemap) nên đây là ESM THUẦN —
// đừng thêm `import.meta.env` hay `process.env` vào file này.
export const BO_HIEN = () => ['thoidai'];
